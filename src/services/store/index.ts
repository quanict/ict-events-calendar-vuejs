import { createStore } from 'vuex'
import photos from "./photos.module";

export default createStore({
  state: {
  },
  getters: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    photos
  }
})
